# Live discovery uses the OpenAI Responses API.
# Keep real credentials out of the repository and evidence.
OPENAI_API_KEY=

# Optional model override. The CLI defaults to a low-latency model for UI discovery.
BYHEART_MODEL=gpt-5.6-luna
